import {useEffect,useState} from 'react';import {motion} from 'framer-motion';import {ShieldCheck} from 'lucide-react';import {Brand} from './Brand';import {useStore} from '../store';
const phases=['Sealing your lesson transcript…','Generating five unseen questions…','Maya is answering alone…','Grading against the concept model…'];
export function Exam(){const [phase,setPhase]=useState(0);const setStage=useStore(s=>s.setStage);const topic=useStore(s=>s.topic);useEffect(()=>{const id=setInterval(()=>setPhase(p=>{if(p>=3){clearInterval(id);setTimeout(()=>setStage('results'),800);return p}return p+1}),850);return()=>clearInterval(id)},[setStage]);return <main className="exam paper"><header className="app-header"><Brand/><div className="blind-badge"><ShieldCheck size={14}/> BLIND EXAM PROTOCOL</div><span/></header><section className="exam-stage"><div className="seal"><div className="orbit o1"/><div className="orbit o2"/><motion.div animate={{rotate:360}} transition={{repeat:Infinity,duration:4,ease:'linear'}} className="examiner-mark">E</motion.div></div><span className="kicker">THE EXAMINER IS NOW WORKING</span><h1>Maya is being tested.</h1><p>The examiner can see Maya’s beliefs—but not a single word of your lesson.</p><div className="phase-list">{phases.map((p,i)=><div className={i<phase?'done':i===phase?'active':''} key={p}><span>{i<phase?'✓':String(i+1).padStart(2,'0')}</span>{p}{i===phase?<i/>:null}</div>)}</div><div className="exam-topic"><small>EXAMINATION SUBJECT</small><b>{topic?.title}</b><span>5 questions · recall, application, mechanism, edge case</span></div></section></main>}
