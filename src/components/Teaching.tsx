import {useEffect,useRef,useState} from 'react';import {ArrowUpRight,Mic,MoreHorizontal,Send,Volume2} from 'lucide-react';import {motion} from 'framer-motion';import {Brand} from './Brand';import {MentalModel} from './MentalModel';import {useStore} from '../store';import {chooseNaturalVoice,playMayaVoice} from '../lib/voice';
export function Teaching(){const {topic,persona,messages,beliefs,teach,setStage,turn,busy,mode,error}=useStore();const [text,setText]=useState('');const [elapsed,setElapsed]=useState(0);const [listening,setListening]=useState(false);const [voiceBusy,setVoiceBusy]=useState(false);const [voice,setVoice]=useState<SpeechSynthesisVoice>();const end=useRef<HTMLDivElement>(null);useEffect(()=>{const id=setInterval(()=>setElapsed(value=>value+1),1000);return()=>clearInterval(id)},[]);useEffect(()=>{const load=()=>setVoice(chooseNaturalVoice(window.speechSynthesis?.getVoices()??[]));load();window.speechSynthesis?.addEventListener('voiceschanged',load);return()=>window.speechSynthesis?.removeEventListener('voiceschanged',load)},[]);useEffect(()=>{end.current?.scrollIntoView({behavior:'smooth'})},[messages,busy]);const send=()=>{if(!text.trim()||busy)return;void teach(text.trim());setText('')};const hearMaya=async(message:string)=>{if(voiceBusy)return;setVoiceBusy(true);await playMayaVoice(message,voice);setVoiceBusy(false)};const speak=()=>{const Recognition=(window as any).SpeechRecognition||(window as any).webkitSpeechRecognition;if(!Recognition){setListening(true);setTimeout(()=>{setListening(false);setText('The mechanism is that ')},900);return}const recognition=new Recognition();recognition.onresult=(event:any)=>setText(event.results[0][0].transcript);recognition.onend=()=>setListening(false);setListening(true);recognition.start()};return <main className="teaching lab-shell"><div className="noise"/><header className="lesson-header"><Brand/><div className="lesson-meta"><span>{topic?.subject}</span><b>{topic?.title}</b><small>{topic?.level}</small></div><div className={`mode-badge ${mode}`}>{mode==='live'?'MAYA IS LISTENING':'MODEL ERROR'}</div><div className="timer"><span className="live-dot"/>{String(Math.floor(elapsed/60)).padStart(2,'0')}:{String(elapsed%60).padStart(2,'0')}</div><button className="quiet" aria-label="Lesson options"><MoreHorizontal/></button></header>{error?<div className="model-error">{error} No response or score was fabricated.</div>:null}<div className="lesson-body"><section className="transcript"><header><div><span className="kicker">YOUR SIDE OF THE DESK</span><h2>Talk it through.</h2></div><span className="persona">MAYA / {persona}</span></header><div className="messages">{messages.map(message=><motion.article initial={{opacity:0,y:12}} animate={{opacity:1,y:0}} className={message.role} key={message.id}><div className="who">{message.role==='teacher'?'YOU':'M'}</div><div><small>{message.role==='teacher'?'YOU EXPLAINED':'MAYA SAID'} / {message.time}</small><p>{message.text}</p>{message.role==='student'?<button className="listen" disabled={voiceBusy} onClick={()=>void hearMaya(message.text)}><Volume2 size={13}/>{voiceBusy?'Maya is finding her voice...':'hear Maya'}</button>:null}</div></motion.article>)}{busy?<article className="student thinking"><div className="who">M</div><div><small>MAYA IS THINKING IT THROUGH</small><p><i/><i/><i/></p></div></article>:null}<div ref={end}/></div><div className="composer"><textarea aria-label="Your explanation" value={text} disabled={busy} onChange={event=>setText(event.target.value)} onKeyDown={event=>{if(event.key==='Enter'&&!event.shiftKey){event.preventDefault();send()}}} placeholder="Explain the next part in your own words..."/><div><button className={`mic ${listening?'active':''}`} onClick={speak}><Mic size={17}/>{listening?'Listening...':'Speak instead'}</button><span>ENTER TO SEND</span><button className="send" disabled={busy} onClick={send} aria-label="Send explanation"><Send size={17}/></button></div></div></section><MentalModel/></div><footer className="lesson-footer"><div><span>{turn.toString().padStart(2,'0')}</span> explanations <i/> <b>{beliefs.filter(belief=>belief.status==='solid').length}</b> ideas landed</div><button disabled={busy} className="test-btn" onClick={()=>setStage('exam')}>I'M DONE — TEST MAYA <ArrowUpRight size={17}/></button></footer></main>}
