import {verifyToken} from '@clerk/backend';
function authorizedParties(){return (process.env.CLERK_AUTHORIZED_PARTIES||'https://protege-lac.vercel.app,http://localhost:5173').split(',').map(value=>value.trim()).filter(Boolean)}
export async function requireAuth(request,response){if(!process.env.CLERK_SECRET_KEY){response.status(503).json({error:'Authentication is not configured on this deployment.'});return null}const authorization=request.headers.authorization||'';if(!authorization.startsWith('Bearer ')){response.status(401).json({error:'Sign in to continue.'});return null}try{return await verifyToken(authorization.slice(7),{secretKey:process.env.CLERK_SECRET_KEY,authorizedParties:authorizedParties()})}catch{response.status(401).json({error:'Your session has expired. Please sign in again.'});return null}}
